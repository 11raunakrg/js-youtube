/// for loop

for (let index = 1; index <= 10; ++index) {
    const element = index;
    console.log(element);
    
}

for(let i = 1;i <=10;i++) {
    // console.log(`${357} * ${i} = ${357 * i}`);
}

///// break and continue

