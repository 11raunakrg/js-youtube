let index = 0;

while(index <= 10) {
    // console.log(`value of index is ${index}`);
    index += 2;

}


let index2 = 1;
while(index2 <= 10) {
    console.log(`${27} * ${index2} is ${27 * index2}`);
    index2++;
}